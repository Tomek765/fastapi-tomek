services:
  - type: web
    name: fastapi-tomek
    env: python
    buildCommand: "pip install fastapi uvicorn openai"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
# dodano bibliotekę openai
